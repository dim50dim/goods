

class Valid2 {
    constructor(email,password,error_message){
        super(email,password);
        this.error_message = error_message;
    }
    validate(){
        super.validate();
        this.error_message = '';

        if(isValide)
    }
}